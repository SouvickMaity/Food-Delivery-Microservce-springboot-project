server:
  port: ${PORT:5008}

spring:
  application:
    name: admin-service
  data:
    mongodb:
      uri:
      database: ${DB_NAME:Food_Delivery}

app:
  jwt:
    secret: ${JWT_SEC:souvick12345678901234567890123456}

