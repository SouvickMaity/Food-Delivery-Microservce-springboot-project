server:
  port: ${PORT:5005}

spring:
  application:
    name: rider-service
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: ${DB_NAME:Food_Delivery}
  rabbitmq:
    addresses: ${RABBITMQ_URL:amqp://admin:admin123@localhost:5672}
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

app:
  jwt:
    secret: ${JWT_SEC:souvick12345678901234567890123456}
  internal-service-key: ${INTERNAL_SERVICE_KEY:jdfienf12345@@@@##4$$$%%%jsadfjlajdf}
  utils-service-url: ${UTILS_SERVICE:http://localhost:5002}
  restaurant-service-url: ${RESTAURANT_SERVICE:http://localhost:5001}
  realtime-service-url: ${REALTIME_SERVICE:http://localhost:5004}
  queues:
    rider: ${RIDER_QUEUE:rider_queue}
    order-ready: ${ORDER_READY_QUEUE:order_ready_queue}
